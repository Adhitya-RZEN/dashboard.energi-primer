# Services boundary

This directory is reserved for business and data service implementations.

Phase 2 intentionally does not add a database client, API client,
authentication service, or dashboard data mock. Those implementations require
the source-of-truth and deployment decisions marked `NEEDS REVIEW` in the
Laravel audit documents.
